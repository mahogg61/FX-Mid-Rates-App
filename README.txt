Mid FX v3.1

Fixes:
- Reference currency menu now populates immediately on app start.
- Changing reference recalculates cross-rates immediately.
- If the reference is not USD, USD is automatically shown as a cross-rate row.
- Decimal selector now forces exactly 4, 5 or 6 decimal places on every displayed FX rate.
- Service worker cache bumped to v3.1.

GitHub update:
Replace index.html, manifest.webmanifest and sw.js with these files and commit.
