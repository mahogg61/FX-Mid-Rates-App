Mid FX v4.1.9

Built from v4.1.8, retaining the v4.1.7 working layout/chart baseline.

Change:
- Compact live status bar.
- Removed the permanent market/quote-method explanatory line.
- Normal display now shows only feed status, actual quote time and refresh countdown.
- Fallback warning remains visible when a reference fallback is used.
- BiQuote live-rate engine unchanged.
- Charts, RSI, 1Y default and all rate logic unchanged.

GitHub update:
Replace index.html, manifest.webmanifest and sw.js and commit.
